rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow read & write to conversions for signed-in users
    match /conversions/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}